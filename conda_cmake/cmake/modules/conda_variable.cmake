
if (NOT CONDA_PUBLIC_LIBS)
    set(CONDA_PUBLIC_LIBS "")
endif ()

if (NOT CONDA_SHARED__LIBS)
    set(CONDA_SHARED__LIBS "")
endif ()

if (NOT CONDA_STATIC_LIBS)
    set(CONDA_STATIC_LIBS "")
endif ()

if (NOT CONDA_INCLUDE_DIRS)
    set(CONDA_INCLUDE_DIRS "")
endif ()